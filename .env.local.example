# Environment Variables

# Database Connection
# For local development with Docker: 
DATABASE_URL=postgresql://harmonia:harmonia_dev_password@localhost:5432/harmonia

# For production (e.g., Vercel with Neon/Supabase):
# DATABASE_URL=postgresql://user:password@host:5432/database

# Individual database connection variables (if required by deployment platform)
DATABASE_HOST=your-database-host
DATABASE_USER=your-database-user
DATABASE_PASSWORD=your-database-password
DATABASE_NAME=your-database-name

# Supabase URLs (if using Supabase)
POSTGRES_URL=your-pooled-connection-url
POSTGRES_URL_NON_POOLING=your-direct-connection-url
POSTGRES_PRISMA_URL=your-prisma-connection-url

# Supabase Keys (if using Supabase)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
SUPABASE_JWT_SECRET=your-jwt-secret

NODE_ENV=development
